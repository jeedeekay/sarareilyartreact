


// function importAll(r) {
//     let images = {};
//     r.keys().map((item, index) => { GALLERY[item.replace('./', '')] = r(item); });
//     return images;
//   };

//  export const GALLERY = importAll(require.context('../assets/imgs/thumbnails/digital', false, /\.(png|jpe?g|svg)$/));